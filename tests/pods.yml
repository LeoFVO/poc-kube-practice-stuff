apiVersion: v1
kind: Pod
metadata:
  name: nginx-allow-privileged
spec:
  containers:
    - name: nginx
      image: nginx:latest
      securityContext:
        runAsUser: 0
        allowPrivilegeEscalation: true
